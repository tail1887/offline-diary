import { getLoggedInUser } from './user.js';

// 로그인/회원가입/메인 표시/숨김 제어 코드를 모두 삭제
// 필요하다면 앱 전체 초기화, 기타 로직만 남기세요

// 예시: 앱 초기화가 필요하다면 아래처럼 작성
document.addEventListener('DOMContentLoaded', () => {
  // 앱 초기화 코드만 작성 (UI 표시/숨김은 ui.js에서만 처리)
});